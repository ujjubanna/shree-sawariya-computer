:root{
  --bg: #070A12;
  --bg2: #0B1022;
  --card: rgba(255, 255, 255, 0.06);
  --card2: rgba(255, 255, 255, 0.08);
  --stroke: rgba(255, 255, 255, 0.10);
  --text: rgba(255, 255, 255, 0.92);
  --muted: rgba(255, 255, 255, 0.70);
  --muted2: rgba(255, 255, 255, 0.58);
  --accent: #7C5CFF;
  --accent2: #2DE2E6;
  --shadow: 0 18px 45px rgba(0,0,0,.35);
  --radius: 18px;
}

*{ box-sizing: border-box; }
html, body{ height: 100%; }
body{
  margin: 0;
  color: var(--text);
  background:
    radial-gradient(1200px 700px at 10% 10%, rgba(124,92,255,.26), transparent 55%),
    radial-gradient(900px 520px at 85% 20%, rgba(45,226,230,.18), transparent 55%),
    radial-gradient(1000px 600px at 50% 90%, rgba(124,92,255,.12), transparent 60%),
    linear-gradient(180deg, var(--bg), var(--bg2));
  font-family: "Plus Jakarta Sans", system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
  line-height: 1.45;
}

a{ color: inherit; }

.container{
  width: min(1120px, 100% - 32px);
  margin: 0 auto;
}

.skip{
  position: absolute;
  left: -999px;
  top: 12px;
  padding: 10px 12px;
  background: rgba(255,255,255,.10);
  border: 1px solid var(--stroke);
  border-radius: 12px;
}
.skip:focus{ left: 16px; z-index: 9999; }

.site-header{
  position: sticky;
  top: 0;
  z-index: 50;
  backdrop-filter: blur(10px);
  background: rgba(7, 10, 18, 0.55);
  border-bottom: 1px solid rgba(255,255,255,0.08);
}

.header-inner{
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16px;
  padding: 14px 0;
}

.brand{
  display: flex;
  gap: 12px;
  align-items: center;
  min-width: 0;
}

.brand-mark{
  width: 42px;
  height: 42px;
  border-radius: 14px;
  background:
    radial-gradient(14px 14px at 30% 30%, rgba(255,255,255,.55), transparent 60%),
    linear-gradient(135deg, rgba(124,92,255,.95), rgba(45,226,230,.85));
  box-shadow: 0 10px 24px rgba(124,92,255,.22);
  border: 1px solid rgba(255,255,255,.14);
  flex: 0 0 auto;
}

.brand-kicker{
  margin: 0;
  font-size: 12px;
  letter-spacing: .12em;
  text-transform: uppercase;
  color: var(--muted2);
}

.brand-name{
  margin: 0;
  font-family: "Fraunces", ui-serif, Georgia, serif;
  font-size: 18px;
  line-height: 1.15;
  letter-spacing: -0.01em;
}

.nav{
  display: flex;
  gap: 10px;
  flex-wrap: wrap;
}

.nav-link{
  text-decoration: none;
  padding: 9px 12px;
  border-radius: 999px;
  border: 1px solid transparent;
  color: var(--muted);
  transition: background .2s ease, border-color .2s ease, color .2s ease, transform .2s ease;
}

.nav-link:hover{
  background: rgba(255,255,255,.06);
  border-color: rgba(255,255,255,.10);
  color: var(--text);
  transform: translateY(-1px);
}

.main{ padding-bottom: 36px; }

.hero{
  padding: 56px 0 18px;
}

.hero-inner{
  display: grid;
  grid-template-columns: 1.35fr .65fr;
  gap: 20px;
  align-items: start;
}

.hero-copy{
  padding: 26px;
  border-radius: var(--radius);
  background: linear-gradient(180deg, rgba(255,255,255,.08), rgba(255,255,255,.05));
  border: 1px solid rgba(255,255,255,.10);
  box-shadow: var(--shadow);
  position: relative;
  overflow: hidden;
}

.hero-copy::before{
  content: "";
  position: absolute;
  inset: -1px;
  background:
    radial-gradient(800px 300px at 30% 0%, rgba(124,92,255,.26), transparent 55%),
    radial-gradient(700px 280px at 90% 20%, rgba(45,226,230,.14), transparent 55%);
  pointer-events: none;
}

.hero-copy > *{ position: relative; }

.pill{
  display: inline-flex;
  align-items: center;
  gap: 8px;
  padding: 8px 12px;
  margin: 0 0 14px;
  border-radius: 999px;
  border: 1px solid rgba(255,255,255,.12);
  background: rgba(255,255,255,.05);
  color: var(--muted);
  font-size: 13px;
}

.hero-title{
  margin: 0 0 12px;
  font-family: "Fraunces", ui-serif, Georgia, serif;
  font-size: clamp(30px, 3.2vw, 44px);
  line-height: 1.05;
  letter-spacing: -0.02em;
}

.hero-subtitle{
  margin: 0 0 18px;
  color: var(--muted);
  font-size: 16px;
}

.cta-row{
  display: flex;
  gap: 12px;
  flex-wrap: wrap;
}

.btn{
  text-decoration: none;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  padding: 12px 14px;
  border-radius: 14px;
  border: 1px solid rgba(255,255,255,.12);
  transition: transform .18s ease, background .18s ease, border-color .18s ease, box-shadow .18s ease;
  font-weight: 600;
}

.btn:active{ transform: translateY(1px); }

.btn-primary{
  background: linear-gradient(135deg, rgba(124,92,255,.95), rgba(45,226,230,.85));
  border-color: rgba(255,255,255,.14);
  box-shadow: 0 14px 30px rgba(124,92,255,.25);
}

.btn-primary:hover{ box-shadow: 0 16px 36px rgba(124,92,255,.32); }

.btn-ghost{
  background: rgba(255,255,255,.06);
}

.btn-ghost:hover{
  background: rgba(255,255,255,.10);
  border-color: rgba(255,255,255,.16);
}

.quick-info{
  margin-top: 18px;
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 10px;
}

.qi{
  padding: 12px;
  border-radius: 14px;
  background: rgba(255,255,255,.05);
  border: 1px solid rgba(255,255,255,.09);
}

.qi-label{
  margin: 0 0 6px;
  font-size: 12px;
  color: var(--muted2);
  letter-spacing: .08em;
  text-transform: uppercase;
}

.qi-value{
  margin: 0;
  font-weight: 700;
  letter-spacing: -.01em;
}

.hero-card{
  border-radius: var(--radius);
  background: linear-gradient(180deg, rgba(255,255,255,.08), rgba(255,255,255,.04));
  border: 1px solid rgba(255,255,255,.10);
  box-shadow: var(--shadow);
  padding: 18px;
}

.hero-card-top{ padding: 6px 6px 10px; }

.card-title{
  margin: 0;
  font-family: "Fraunces", ui-serif, Georgia, serif;
  letter-spacing: -0.01em;
}

.card-sub{
  margin: 6px 0 0;
  color: var(--muted);
  font-size: 14px;
}

.contact-list{
  display: grid;
  gap: 10px;
  margin-top: 10px;
}

.contact-item{
  text-decoration: none;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 10px;
  padding: 12px;
  border-radius: 14px;
  border: 1px solid rgba(255,255,255,.10);
  background: rgba(255,255,255,.05);
  transition: transform .18s ease, background .18s ease, border-color .18s ease;
}

.contact-item:hover{
  transform: translateY(-1px);
  background: rgba(255,255,255,.08);
  border-color: rgba(255,255,255,.14);
}

.contact-label{
  color: var(--muted2);
  font-size: 12px;
  letter-spacing: .08em;
  text-transform: uppercase;
}

.contact-value{
  font-weight: 700;
}

.divider{
  height: 1px;
  margin: 14px 0;
  background: rgba(255,255,255,.10);
}

.note{
  margin: 0;
  color: var(--muted);
  font-size: 13px;
}

.section{
  padding: 34px 0;
}

.section-head{
  margin-bottom: 14px;
}

.section-title{
  margin: 0;
  font-family: "Fraunces", ui-serif, Georgia, serif;
  letter-spacing: -0.01em;
  font-size: 28px;
}

.section-sub{
  margin: 8px 0 0;
  color: var(--muted);
}

.grid{
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 14px;
}

.card{
  padding: 18px;
  border-radius: var(--radius);
  background: rgba(255,255,255,.05);
  border: 1px solid rgba(255,255,255,.10);
  box-shadow: 0 12px 30px rgba(0,0,0,.22);
  transition: transform .18s ease, background .18s ease, border-color .18s ease;
}

.card:hover{
  transform: translateY(-2px);
  background: rgba(255,255,255,.07);
  border-color: rgba(255,255,255,.14);
}

.card-h{
  margin: 0 0 8px;
  font-size: 18px;
  font-weight: 800;
}

.card-p{
  margin: 0 0 12px;
  color: var(--muted);
}

.list{
  margin: 0;
  padding-left: 18px;
  color: rgba(255,255,255,.78);
}

.list li{ margin: 6px 0; }

.section-alt{ padding-top: 14px; }

.cta-slab{
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 14px;
  padding: 18px;
  border-radius: var(--radius);
  background: linear-gradient(135deg, rgba(124,92,255,.20), rgba(45,226,230,.12));
  border: 1px solid rgba(255,255,255,.12);
}

.footer{
  padding: 18px 0 24px;
  border-top: 1px solid rgba(255,255,255,.08);
  background: rgba(7,10,18,.35);
}

.footer-inner{
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12px;
  flex-wrap: wrap;
}

.footer-text{
  margin: 0;
  color: var(--muted);
  font-size: 14px;
}

.footer-links{
  display: flex;
  align-items: center;
  gap: 10px;
  color: rgba(255,255,255,.78);
}

.footer-link{
  text-decoration: none;
  padding: 8px 10px;
  border-radius: 999px;
  border: 1px solid rgba(255,255,255,.10);
  background: rgba(255,255,255,.04);
}

.footer-link:hover{
  background: rgba(255,255,255,.08);
  border-color: rgba(255,255,255,.14);
}

.dot{ opacity: .6; }

@media (max-width: 920px){
  .hero-inner{ grid-template-columns: 1fr; }
  .hero{ padding-top: 26px; }
  .brand-name{ font-size: 16px; }
}

@media (max-width: 720px){
  .grid{ grid-template-columns: 1fr; }
  .cta-slab{ flex-direction: column; align-items: stretch; }
  .btn{ width: 100%; }
  .nav{ display: none; }
}
